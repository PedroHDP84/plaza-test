/**
 * TODO:
 * [ ] Combine all icons into 1 component with variants
 * [ ] Add classes to icons to allow for styling
 */

import SendIcon from "./Send";
import Assistant from "./Assistant";
import User from "./User";

export { SendIcon, Assistant, User };
